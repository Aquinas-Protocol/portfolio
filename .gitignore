# build output
dist/
# local lighthouse audit output (not committed)
.lighthouse/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# cloudflare local state + local-only secrets (never commit .dev.vars)
.wrangler/
.dev.vars

# pitch-agent KB drafts — prose stays local until approved (only kb/manifest.json + kb/facts.json are tracked)
kb/source/

# pitch-agent internal build handoff — working doc (local paths, TODOs, detection internals); not a public artifact
PITCH-AGENT-HANDOFF.md

# pitch-agent DB tooling — migrations, ingest script, Neon runbook stay local (applied manually to Neon; not needed by the Worker deploy)
db/
